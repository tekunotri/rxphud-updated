// Load the base configuration
//exec default.cfg

// Run a user script file if present
exec autoexec.cfg

// Stuff command line statements
stuffcmds

// Display the startup level
startupmenu
sv_unlockedchapters 99

// Apply HUD settings
cl_hud_minmode 0 // minmode isn't supported nor can i be fucked to fix it ever
